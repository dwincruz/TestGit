@model VMNS.Models.Sub_UnderVehicle
@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Body.cshtml";
    var currentValues = new[] { "Good for Service", "For Monitoring", "Needs Repair" };

}
<form asp-action="Edit">
    <div class="row mt-3">
        <input type="hidden" asp-for="Id" />
        <div class="col-md-6 col-sm-4 col-6 my-auto">
            <h2><i class="ri-car-fill text-lra-primary"></i>  Under Vehicle</h2>
        </div>
        <div class="col-md-6 col-sm-6 col-6  my-auto position-relative">
            <div class="float-end">
                <a asp-area="" asp-controller="Maintenances" asp-action="Details" asp-route-Id=@ViewData["MaintenanceId"] class="btn btn-sm text-capitalize bg-white box-lra-shadow rounded"><i class="ri-arrow-go-back-line"></i> Go Back</a>
                <button type="submit" class="btn btn-sm text-white text-capitalize bg-lra-primary box-lra-shadow rounded "><i class="ri-save-line"></i> Save</button>
            </div>
        </div>
        <div class="col-12">
            <div asp-validation-summary="All" class="text-danger"></div>
        </div>
        <div class="row bg-white box-lra-shadow rounded d-flex justify-content-center pt-4">
            <div class="col-lg-7 col-sm-7 col-12">
            <div class="row">
                  <div class="col-12">
                    <input type="text"
                           asp-for="MaintenanceId"
                           id=""
                           class="form-control"
                           required
                           readonly
                           hidden
                           pattern="\S+.*" />
                    <table class="table  table-borderless">
                        <thead>
                            <tr>
                                <th colspan="4" class="good text-white">
                                    Good for Service
                                </th>
                            </tr>
                            <tr>
                                <th class="good"></th>
                                <th colspan="3" class="warning">
                                    For Monitoring
                                </th>
                            </tr>
                            <tr>
                                <th class="good"></th>
                                <th class="warning"></th>
                                <th colspan="2" class="danger text-white">
                                    Needs Repair
                                </th>
                            </tr>
                            <tr>
                                <th colspan="4" class="category-name">
                                    Undervehicle
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="TransmissionFluid" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.TransmissionFluid == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.TransmissionFluid)
                                    <input type="radio" name="TransmissionFluid" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.TransmissionFluid == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="Coolant" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Coolant == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.Coolant)
                                    <input type="radio" name="Coolant" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Coolant == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="ExhaustPipes" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ExhaustPipes == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.ExhaustPipes)
                                    <input type="radio" name="ExhaustPipes" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ExhaustPipes == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="TransferCase" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.TransferCase == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.TransferCase)
                                    <input type="radio" name="TransferCase" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.TransferCase == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="FluidLinesConnections" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.FluidLinesConnections == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.FluidLinesConnections)
                                    <input type="radio" name="FluidLinesConnections" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.FluidLinesConnections == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="Wiring" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Wiring == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.Wiring)
                                        <input type="radio" name="Wiring" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Wiring == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="EngineOil" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.EngineOil == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.EngineOil)
                                        <input type="radio" name="EngineOil" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.EngineOil == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="ParkingBrakeCable" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ParkingBrakeCable == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.ParkingBrakeCable)
                                        <input type="radio" name="ParkingBrakeCable" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ParkingBrakeCable == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="NutsAndBolts" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.NutsAndBolts == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.NutsAndBolts)
                                        <input type="radio" name="NutsAndBolts" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.NutsAndBolts == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="Muffler" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Muffler == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.Muffler)
                                        <input type="radio" name="Muffler" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Muffler == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="ShockAbsorber" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ShockAbsorber == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.ShockAbsorber)
                                        <input type="radio" name="ShockAbsorber" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ShockAbsorber == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="DriveShaftBoots" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.DriveShaftBoots == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.DriveShaftBoots)
                                        <input type="radio" name="DriveShaftBoots" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.DriveShaftBoots == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="TransmissionAssembly" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.TransmissionAssembly == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.TransmissionAssembly)
                                        <input type="radio" name="TransmissionAssembly" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.TransmissionAssembly == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="UJoints" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.UJoints == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.UJoints)
                                        <input type="radio" name="UJoints" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.UJoints == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="DustCover" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.DustCover == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.DustCover)
                                        <input type="radio" name="DustCover" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.DustCover == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="Hoses" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Hoses == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.Hoses)
                                        <input type="radio" name="Hoses" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Hoses == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="FuelTankVaporVentSystemsHoses" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.FuelTankVaporVentSystemsHoses == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.FuelTankVaporVentSystemsHoses)
                                        <input type="radio" name="FuelTankVaporVentSystemsHoses" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.FuelTankVaporVentSystemsHoses == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="FluidTankBand" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.FluidTankBand == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.FluidTankBand)
                                        <input type="radio" name="FluidTankBand" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.FluidTankBand == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="AutomaticTransmissionFluid" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.AutomaticTransmissionFluid == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.AutomaticTransmissionFluid)
                                        <input type="radio" name="AutomaticTransmissionFluid" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.AutomaticTransmissionFluid == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="BallJoints" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.BallJoints == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.BallJoints)
                                        <input type="radio" name="BallJoints" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.BallJoints == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="BrakeLines" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.BrakeLines == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.BrakeLines)
                                        <input type="radio" name="BrakeLines" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.BrakeLines == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="CatalyticConverter" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.CatalyticConverter == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.CatalyticConverter)
                                        <input type="radio" name="CatalyticConverter" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.CatalyticConverter == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="ConstantVelocityBoots" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ConstantVelocityBoots == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.ConstantVelocityBoots)
                                        <input type="radio" name="ConstantVelocityBoots" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ConstantVelocityBoots == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                    <td class="checkbox-container">
                                        <input type="radio" name="Differential" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Differential == currentValue ? "checked" : "") />
                                    </td>
                                }
                                <td>
                                    @Html.DisplayNameFor(model => model.Differential)
                                        <input type="radio" name="Differential" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Differential == "Not Inspected" ? "checked" : "") />
                                </td>
                            </tr>
                            <tr>
                                <td colspan="4" class="category-name">
                                    Remarks
                                </td>
                            </tr>
                            <tr>
                                <td colspan="4">
                                    <textarea class="form-control" placeholder="Leave a comment here" asp-for="Remarks">
                                    </textarea>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                  </div>
            </div>
              
        </div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

