@model VMNS.Models.Sub_InteriorExterior

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Body.cshtml";
    var currentValues = new[] { "Good for Service", "For Monitoring", "Needs Repair" };

}
<form class="row mt-3" asp-action="Create">
    <div class="col-md-6 col-sm-4 col-6 my-auto">
        <h2><i class="ri-car-fill text-lra-primary"></i> Interior / Exterior</h2>
    </div>
    <div class="col-md-6 col-sm-6 col-6  my-auto position-relative">
        <div class="float-end">
            <a asp-area="" asp-controller="Maintenances" asp-action="Details" asp-route-Id=@ViewData["MaintenanceId"] class="btn btn-sm text-capitalize bg-white box-lra-shadow rounded"><i class="ri-arrow-go-back-line"></i> Go Back</a>
            <button asp-action="Create" type="submit" class="btn btn-sm  text-white text-capitalize bg-lra-primary box-lra-shadow rounded "><i class="ri-save-line"></i> Save</button>
        </div>
    </div>
    <div class="col-12">
        <div asp-validation-summary="All" class="text-danger"></div>
    </div>
    <div class="row bg-white box-lra-shadow rounded d-flex justify-content-center pt-4">
        <div class="col-lg-7 col-sm-7 col-12">
        <div class="row">
            <div class="col-12">
                <input type="text"
                       asp-for="MaintenanceId"
                       id=""
                       class="form-control"
                       required
                       readonly
                       hidden
                       pattern="\S+.*" />
                <table class="table table-borderless">
                    <thead>
                        <tr>
                            <th colspan="4" class="good text-white">
                                Good for Service
                            </th>
                        </tr>
                        <tr>
                            <th class="good"></th>
                            <th colspan="3" class="warning">
                                For Monitoring
                            </th>
                        </tr>
                        <tr>
                            <th class="good"></th>
                            <th class="warning"></th>
                             <th colspan="2" class="danger text-white">
                                Needs Repair
                            </th>
                        </tr>
                        <tr>
                            <th colspan="4" class="category-name">
                                Interior / Exterior
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="HeadLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.HeadLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="TailLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.TailLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="TurnSignals" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.TurnSignals)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="BrakeLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.BrakeLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="HazardLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.HazardLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="ExteriorLamps" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.ExteriorLamps)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="LicensePlatesLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.LicensePlatesLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="WindshieldWiper" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.WindshieldWiper)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="WiperBlades" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.WiperBlades)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="WindshieldCondition" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.WindshieldCondition)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="EmergencyBrakeAdjustment" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.EmergencyBrakeAdjustment)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="HornOperation" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.HornOperation)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="FuelTankCapBasket" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.FuelTankCapBasket)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="AirConGasket" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.AirConGasket)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="AirConFilter" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.AirConFilter)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="ClutchOperation" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.ClutchOperation)</td>
                        </tr>
                        
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="BackupLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.BackupLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="DashWarningLights" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.DashWarningLights)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="Carpet" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.Carpet)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="GlassAndMirrors" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.GlassAndMirrors)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="Wiring" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.Wiring)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="Odometer" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.Odometer)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="FuelGuage" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.FuelGuage)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="BatteryVoltage" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.BatteryVoltage)</td>
                        </tr>
                        <tr class="condition-row">
                            @foreach (var currentValue in currentValues)
                            {
                                <td class="checkbox-container">
                                    <input type="radio" name="Indicators" id="@currentValue" class="checkbox-item" value="@currentValue" />
                                </td>
                            }
                            <td>@Html.DisplayNameFor(model => model.Indicators)</td>
                        </tr>
                        <tr>
                            <td colspan="4" class="category-name">
                                Remarks
                            </td>
                        </tr>
                        <tr>
                            <td colspan="4">
                                <textarea class="form-control" placeholder="Leave a comment here" asp-for="Remarks">
                                    </textarea>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    </div>
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
