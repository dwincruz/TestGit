@model VMNS.Models.Sub_UnderHood

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Body.cshtml";
    var currentValues = new[] { "Good for Service", "For Monitoring", "Needs Repair" };

}
<form asp-action="Edit">
    <div class="row mt-3">
        <input type="hidden" asp-for="Id" />
        <div class="col-md-6 col-sm-4 col-6 my-auto">
            <h2><i class="ri-steering-2-line text-lra-primary"></i> Under Hood</h2>
        </div>
        <div class="col-md-6 col-sm-6 col-6  my-auto position-relative">
            <div class="float-end">
                <a asp-area="" asp-controller="Maintenances" asp-action="Details" asp-route-Id=@ViewData["MaintenanceId"] class="btn btn-sm text-capitalize bg-white box-lra-shadow rounded"><i class="ri-arrow-go-back-line"></i> Go Back</a>
                <button type="submit" class="btn btn-sm text-white text-capitalize bg-lra-primary box-lra-shadow rounded "><i class="ri-save-line"></i> Save</button>
            </div>
        </div>
        <div class="col-12">
            <div asp-validation-summary="All" class="text-danger"></div>
        </div>
        <div class="row bg-white box-lra-shadow rounded d-flex justify-content-center pt-4">
            <div class="col-lg-7 col-sm-7 col-12">
                <div class="row">
                    <input type="text"
                            asp-for="MaintenanceId"
                            id=""
                            class="form-control"
                            required
                            readonly
                            hidden
                            pattern="\S+.*" />
                    <div class="col-12">
                        <table class="table  table-borderless">
                            <thead>
                                <tr>
                                    <th colspan="4" class="good text-white">
                                        Good for Service
                                    </th>
                                </tr>
                                <tr>
                                    <th class="good"></th>
                                    <th colspan="3" class="warning">
                                        For Monitoring
                                    </th>
                                </tr>
                                <tr>
                                    <th class="good"></th>
                                    <th class="warning"></th>
                                    <th colspan="2" class="danger text-white">
                                        Needs Repair
                                    </th>
                                </tr>
                                <tr>
                                    <th colspan="4" class="category-name">
                                        Underhood
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="condition-row">
                                @foreach (var currentValue in currentValues)
                                {
                                        <td class="checkbox-container">
                                            <input type="radio" name="FluidLevels" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.FluidLevels == currentValue ? "checked" : "") />
                                    
                                    </td>
                                }
                                    <td>
                                        @Html.DisplayNameFor(model => model.FluidLevels)
                                        <input type="radio" name="FluidLevels" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.FluidLevels == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="HeaterHoses" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.HeaterHoses == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td> 
                                        @Html.DisplayNameFor(model => model.HeaterHoses)
                                        <input type="radio" name="HeaterHoses" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.HeaterHoses == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="AirCondition" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.AirCondition == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.AirCondition)
                                        <input type="radio" name="AirCondition" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.AirCondition == "Not Inspected" ? "checked" : "") />
                                    </td>

                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="DriveBelt" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.DriveBelt == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.DriveBelt)
                                        <input type="radio" name="DriveBelt" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.DriveBelt == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="ComputerBox" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ComputerBox == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.ComputerBox)
                                        <input type="radio" name="ComputerBox" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ComputerBox == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="ElectricFuses" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ElectricFuses == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                         @Html.DisplayNameFor(model => model.ElectricFuses)
                                        <input type="radio" name="ElectricFuses" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ElectricFuses == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="Alternator" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Alternator == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                         @Html.DisplayNameFor(model => model.Alternator)
                                        <input type="radio" name="Alternator" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Alternator == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="CoolantRecoveryReservoirFluidLevel" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.CoolantRecoveryReservoirFluidLevel == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.CoolantRecoveryReservoirFluidLevel)
                                        <input type="radio" name="CoolantRecoveryReservoirFluidLevel" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.CoolantRecoveryReservoirFluidLevel == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="AirConditionerCondenser" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.AirConditionerCondenser == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.AirConditionerCondenser)
                                        <input type="radio" name="AirConditionerCondenser" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.AirConditionerCondenser == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="CoolingSystemHoses" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.CoolingSystemHoses == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.CoolingSystemHoses)
                                        <input type="radio" name="CoolingSystemHoses" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.CoolingSystemHoses == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="EngineAirFilter" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.EngineAirFilter == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.EngineAirFilter)
                                        <input type="radio" name="EngineAirFilter" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.EngineAirFilter == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="ClutchReservoirFluid" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.ClutchReservoirFluid == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.ClutchReservoirFluid)
                                        <input type="radio" name="ClutchReservoirFluid" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.ClutchReservoirFluid == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="AuxiliaryBelt" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.AuxiliaryBelt == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.AuxiliaryBelt)
                                        <input type="radio" name="AuxiliaryBelt" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.AuxiliaryBelt == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="Washer" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.Washer == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.Washer)
                                        <input type="radio" name="Washer" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.Washer == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="RadiatorCore" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.RadiatorCore == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.RadiatorCore)
                                        <input type="radio" name="RadiatorCore" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.RadiatorCore == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="PowerSteering" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.PowerSteering == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.PowerSteering)
                                        <input type="radio" name="PowerSteering" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.PowerSteering == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="BrakeFluid" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.BrakeFluid == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.BrakeFluid)
                                        <input type="radio" name="BrakeFluid" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.BrakeFluid == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr class="condition-row">
                                    @foreach (var currentValue in currentValues)
                                    {
                                        <td class="checkbox-container">
                                            <input type="radio" name="HosesandConnections" id="@currentValue" class="checkbox-item" value="@currentValue" @(Model.HosesandConnections == currentValue ? "checked" : "") />
                                        
                                        </td>
                                    }
                                    <td>
                                        @Html.DisplayNameFor(model => model.HosesandConnections)
                                        <input type="radio" name="HosesandConnections" id="" class="checkbox-item d-none" value="Not Inspected" @(Model.HosesandConnections == "Not Inspected" ? "checked" : "") />
                                    </td>
                                </tr>
                                <tr>
                                    <td colspan="4" class="category-name">
                                        Remarks
                                    </td>
                                </tr>
                                <tr>
                                    <td colspan="4">
                                        <textarea class="form-control" placeholder="Leave a comment here"  asp-for="Remarks">
                                        </textarea>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

